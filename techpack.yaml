schemaVersion: 1
identifier: ios
displayName: "iOS Development"
description: Xcode integration, simulator management, and Apple documentation for Claude Code
author: Bruno Guidolim
minMCSVersion: "2026.3.6"

# ---------------------------------------------------------------------------
# Prompts — resolved interactively during `mcs sync`
# ---------------------------------------------------------------------------
prompts:
  - key: PROJECT
    type: fileDetect
    label: "Xcode project / workspace"
    detectPattern:
      - "*.xcworkspace"
      - "*.xcodeproj"

# ---------------------------------------------------------------------------
# Components
# ---------------------------------------------------------------------------
components:
  # ── Dependencies ────────────────────────────────────────────────────────
  - id: homebrew
    displayName: Homebrew
    description: macOS package manager
    type: brewPackage
    shell: '/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"'
    doctorChecks:
      - type: commandExists
        name: Homebrew
        section: Dependencies
        command: brew

  - id: jq
    description: JSON processor (used by simulator hook)
    dependencies: [homebrew]
    brew: jq

  - id: mobilebuildmcp-binary
    displayName: MobileBuildMCP binary
    description: Provides the binary the MobileBuildMCP server runs
    dependencies: [homebrew]
    brew: getsentry/xcodebuildmcp/mobilebuildmcp

  # ── MCP Servers ─────────────────────────────────────────────────────────
  - id: mobilebuildmcp
    displayName: MobileBuildMCP
    description: MCP server for Xcode build, test, and simulator workflows
    dependencies: [mobilebuildmcp-binary]
    mcp:
      name: MobileBuildMCP
      command: mobilebuildmcp
      args:
        - "mcp"
      env:
        MOBILEBUILDMCP_SENTRY_DISABLED: "true"

  - id: sosumi
    displayName: Sosumi
    description: Apple documentation search via MCP (HTTP transport)
    mcp:
      url: https://sosumi.ai/mcp

  # ── Hooks ───────────────────────────────────────────────────────────────
  - id: hook-ios-simulator
    displayName: iOS simulator status hook
    description: Reports booted iOS simulator UUID on session start
    dependencies: [jq]
    hookEvent: SessionStart
    hook:
      source: hooks/ios-simulator-status.sh
      destination: ios-simulator-status.sh

  # ── Configuration ───────────────────────────────────────────────────────
  - id: gitignore
    displayName: Gitignore
    description: Ignores MobileBuildMCP config directory
    isRequired: true
    gitignore:
      - .mobilebuildmcp

# ---------------------------------------------------------------------------
# Templates — CLAUDE.local.md sections
# ---------------------------------------------------------------------------
templates:
  - sectionIdentifier: ios
    contentFile: templates/ios.md

  - sectionIdentifier: mobilebuildmcp
    contentFile: templates/mobilebuildmcp.md
    dependencies:
      - mobilebuildmcp

# ---------------------------------------------------------------------------
# Configure project script — runs during `mcs sync`
# ---------------------------------------------------------------------------
configureProject:
  script: scripts/configure-xcode.sh

# ---------------------------------------------------------------------------
# Supplementary doctor checks
# ---------------------------------------------------------------------------
supplementaryDoctorChecks:
  - type: commandExists
    name: Xcode Command Line Tools
    section: iOS
    command: xcode-select
    args: ["-p"]
    fixCommand: "xcode-select --install"
